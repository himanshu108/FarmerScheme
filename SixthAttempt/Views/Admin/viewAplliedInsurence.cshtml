@model IEnumerable<SixthAttempt.Models.Insurence>

@{
    ViewBag.Title = "viewAplliedInsurence";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>viewAplliedInsurence</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.companyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.city)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ownerOfCompany)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.companyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.city)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ownerOfCompany)
        </td>
       
    </tr>
}

</table>
